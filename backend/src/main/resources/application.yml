server:
  port: ${PORT:4000}
spring:
  application:
    name: decorai-api
  flyway:
    enabled: true
    locations: classpath:db/migration
  jackson:
    default-property-inclusion: non_null
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
logging:
  level:
    org.springframework.jdbc.core: warn

# Architecture: modular microservices behind this gateway (see MICROSERVICES.md)
decorai:
  architecture: modular-microservices

# Paste only secrets into root .env — never commit them.
# GEMINI_API_KEY=...          # Google AI Studio — enables /ai/decorate
# DATABASE_URL=postgresql://...@ep-xxx-pooler....neon.tech/neondb?sslmode=require
# PAYSTACK_SECRET_KEY=sk_test_...
# MAIL_USERNAME=your@gmail.com
# MAIL_PASSWORD=gmail-app-password
# PRO_PLAN_AMOUNT_PESWAS=5000
