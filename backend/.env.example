# Copy these values into the repository .env or set them in your shell before starting Spring Boot.
# Neon supplies a postgresql:// URL; the application converts it to JDBC automatically.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require
PORT=4000
